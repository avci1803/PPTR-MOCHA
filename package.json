{
  "name": "pptr-mocha",
  "version": "1.0.0",
  "description": "test framework with mocha steps and the other class pattern",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --timeout=30000 --require mocha-steps ./build/tests/**.js",
    "clean": "rm -rf build",
    "build": "babel --preset es2015 -d build/ src/"
  },
  "keywords": [
    "mocha",
    "puppeteer",
    "framework"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0",
    "mocha-steps": "^1.3.0",
    "puppeteer": "^21.3.8"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
